query getProductsByIds($ids: [String!]!) {
	Product(where: { Id: { in: $ids } }) {
		items {
			BreadcrumbContainer {
				BreadCrumbs {
					PageId
					PageUrl
					PageTitle
				}
			}
			...ProductData
		}
	}
}
