
import React from 'react';
import { CartTotalProps } from './types';

const CartTotal: React.FC<CartTotalProps> = (props) => {
  // console.log(`CartTotal props:`, props);
  return (
    <>
      <h1>CartTotal</h1>
      {/* Render more fields from CartTotalData here */}
    </>
  );
};

export default CartTotal;
